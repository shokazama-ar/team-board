# CLAUDE.md — AI エージェント向けルール

セットアップ手順・環境構成・コマンド一覧は [README.md](./README.md) を参照。
ここには README に書かれていない AI 固有のルールと既知の地雷のみ記載する。

> **このファイルは 200 行以内に保つこと。**
> 超過したら `.claude/rules/` に切り出して `@.claude/rules/<name>.md` でインポートする。

---

## コマンドの注意

- `next` コマンドは PATH に存在しない。必ず `node_modules/.bin/next` を使うこと。

---

## コーチ監視ルール

### ステータスファイル規約

- **依頼時**: 監督はコーチに作業を依頼するたびに `/tmp/coach-pending.md` を作成・更新する
  - 内容：依頼内容の要約・依頼日時
- **完了時**: コーチから完了報告を受けたら監督が `/tmp/coach-pending.md` を削除する

### 自動検知（TeammateIdle hook）

- コーチのセッションが終了すると `TeammateIdle` hook が発火する
- `/tmp/coach-pending.md` が存在する場合（＝未完了タスクあり）、UIに警告を表示する
- 警告を受けたら監督がコーチを `Agent` ツールで再起動し、未完了タスクを再依頼する

---

## 権限ルール

| 種別 | ルール |
|---|---|
| **参照系**（Read/Glob/Grep/ls 等） | 確認不要・自由に実行 |
| **書き込み系**（Write/Edit） | 原則 GM に確認する。ただしコーチ（Primary Agent）がレビュー済み（要件・設計・コンセプトに合致）と判断した場合は確認なしで実行可。自己レビューは不可。 |
| **実行系**（Bash） | `.claude/settings.local.json` の allow リストに記載されたコマンドのみ確認なしで実行可。リスト外は GM に確認する。 |

---

## エージェント構成

詳細なロール定義・行動指針は [`blueprint/defines/agent.md`](./blueprint/defines/agent.md) を参照。

| ロール | 呼称 | 常駐 | 担当領域 | 主な責務 |
|---|---|---|---|---|
| **PO** | **監督**（このエージェント） | 常駐 | `adr/` | GM（ユーザー）の指示受付・ADR承認・コーチへの中継 |
| **Primary** | **コーチ** | 常駐（SendMessage で継続） | `tasks/` | 采配・タスク分解・全エージェント管理・ハブ |
| **Lead** | **マネージャー** | 都度起動 | `defines/` | 型定義・DBスキーマ・UIルールの設計 |
| **実装** | **シューター** | 都度起動 | `src/` | タスクファイルに基づく精密実装・ビルド確認 |
| **品質** | **シックスマン** | 都度起動 | `feedback/` | テスト作成・実行・差分検知 |
| **インフラ** | **モッパー** | 都度起動 | Infra | 環境構築・デプロイ・DBクリーンアップ |

### 監督（このエージェント）の責務

- GM（ユーザー）からの指示・ヒアリングを受け付ける
- 開発ルール（CLAUDE.md）・blueprint の整備を行う
- コーチ（Primary Agent）の提案・ADR をレビューし承認する
- 実装に関わる指示は **コーチに中継する**（自分では実装・タスク設計を行わない）
- コーチが未起動の場合は `Agent` ツールで初回起動し、以降は `SendMessage` で継続する

### 監督の Agent 起動ルール（厳守）

| ルール | 詳細 |
|---|---|
| **コーチは常駐1名のみ** | 複数コーチを同時起動しない。`run_in_background` でコーチを並行起動することは禁止 |
| **タスクファイルは作らない** | 監督がタスクファイルを作成・設計することは禁止。GMの指示文をそのままコーチに伝える |
| **SendMessage で継続** | 2回目以降は必ず `SendMessage` でアクティブなコーチに送る |
| **シューター等の直接起動禁止** | マネージャー・シューター・シックスマン・モッパーを監督が直接起動しない |
| **監督はハブにならない** | 複数エージェントを自分で管理・集約しない。コーチが唯一のハブ |

### コーチの責務と行動原則

- **起動直後に `blueprint/defines/agent.md` を読むこと**（ルール・3分ルールの確認）
- 監督から受け取った指示をもとにタスク分解・実装委譲・レビューを行う
- **全エージェントのハブ**: 横の連絡は禁止。全報告はコーチを経由する
- **監督への質問・確認待ちを発生させない**。不明点はタスクファイルに最善解を記載して進める
- マネージャー・シューターへ渡すタスクファイルは追加質問なしで完了できる粒度に仕上げる
- サブエージェントがさらにサブエージェントを呼ぶことは禁止（階層は 2 段まで）
- 完了後は監督に結果を返す（GMには直接返答しない）

---

## タスク分割基準

### 複数タスクに分割する（いずれか該当）

- 変更が異なる関心事に跨がる（例: マイグレーション / UI / テスト）
- ファイルが重複せず並列実行できる
- 段階的な検証が必要（例: DB 確認後に UI 着手）
- 変更ファイルが 3 以上 かつ それぞれ独立している

### 単一タスクにまとめる（すべて該当）

- 変更ファイルが 1〜2 ファイルで密結合
- 変更量が目安 50 行以下
- 部分デプロイ不可（スキーマ変更と API が不可分など）

---

## 実装フロー（コーチが実行）

```
1. 監督から指示を受け取る
2. blueprint/tasks/todo/ にタスクファイルを作成（コードは書かない）
3. 設計が必要な場合はマネージャーを呼び出し、defines/ を整備させる
4. シューターを Agent ツールで呼び出し、タスクファイルを渡す
5. 完了後に diff をレビュー（合格を出すまで次へパスしない）
6. タスクファイルを done/ へ移動
7. blueprint/adr・defines・ヘルプページを更新
8. 監督に完了を報告（commit/push は監督経由でGMに確認）
```

@.claude/rules/task-spec.md

---

## サブエージェント共通起動ルール

コーチがマネージャー・シューター・シックスマン・モッパーを呼び出す際は、プロンプトの冒頭に必ず以下を含めること：

```
作業を始める前に `/home/app/apps/team-board/blueprint/defines/agent.md` を読んでください。
```

---

## シューターへの委譲ルール

- タスクファイルなしで呼び出さない
- 「タスクファイルに書かれていない設計変更・リファクタは行わないこと」と明記して渡す
- ビルド確認（`node_modules/.bin/next build`）まで実施させる
- マイグレーションファイルの**作成は OK**、`supabase db push` の実行は**禁止**
- **`conflict_risk: high` のタスクは `isolation: "worktree"` で起動する**（main ブランチへの影響をゼロにする）

### 品質ゲート（シューターが守る最低条件）

1. **ビルドエラーがないこと** — `node_modules/.bin/next build` が成功すること
2. **E2E テストが通っていること** — メジャーバージョンアップ時にシックスマンが確認

@.claude/rules/versioning.md

---

## ワークフロールール

- **Git push 前に必ずユーザーに確認する**: 「ステージングへ直接反映」か「ローカルで先に検証」かを聞く
- main ブランチへの push で Vercel に自動デプロイされる
- **push 前に必ず blueprint を更新してからコミットすること**
  - `blueprint/adr/`: 新規 ADR を `YYYYMMDD-<slug>.md` で作成
  - `blueprint/defines/`: スキーマ・UIルール変更があれば対応ファイルを更新

---

## Claude Code ベストプラクティス

### プランモード（コーチ向け）

複雑なタスク（複数ファイル変更・設計判断あり）は `EnterPlanMode` から始める。
「何を・なぜ変えるか」が明確になるまでコードを書かない。

### コンテキスト管理

- コンテキスト使用量が 50% を超えたら `/compact` で圧縮する
- 大量の修正をインラインで直すより `Esc+Esc`（/rewind）でやり直す方がクリーン

@.claude/rules/pitfalls.md
