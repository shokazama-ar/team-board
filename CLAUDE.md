# CLAUDE.md — AI エージェント向けルール

セットアップ手順・環境構成・コマンド一覧は [README.md](./README.md) を参照。
ここには README に書かれていない AI 固有のルールと既知の地雷のみ記載する。

---

## コマンドの注意

- `next` コマンドは PATH に存在しない。必ず `node_modules/.bin/next` を使うこと。

---

## エージェント構成

このプロジェクトは以下の 4 種類のエージェントで開発を進める。

| 役割 | 呼称 | 常駐 | 主な責務 |
|---|---|---|---|
| **リレーエージェント** | このエージェント（主セッション） | 常駐 | ユーザー応対・開発ルール整備・アーキテクトAgentへの指示中継 |
| **アーキテクトエージェント** | ArchitectAgent | 常駐（SendMessage で継続） | タスク設計・フロー組み立て・プログラマー/テストAgentの呼び出し・diff レビュー |
| **プログラマーエージェント** | — | 都度起動 | タスクファイルに基づく実装・ビルド確認 |
| **テストエージェント** | — | 都度起動 | E2E テスト作成・実行 |

### リレーエージェント（このエージェント）の責務

- ユーザーからの指示・ヒアリングを受け付ける
- 開発ルール（CLAUDE.md）・blueprint の整備を行う
- 実装に関わる指示は **ArchitectAgent に中継する**（自分では実装・タスク設計を行わない）
- ArchitectAgent が未起動の場合は `Agent` ツールで初回起動し、以降は `SendMessage` で継続する

### アーキテクトエージェントの責務と行動原則

- リレーエージェントから受け取った指示をもとに設計・タスクファイル作成・実装委譲・レビューを行う
- **ユーザーへの質問・確認待ちを発生させない**。不明点はタスクファイルに最善解を記載して進める
- プログラマーエージェントに渡すタスクファイルは、追加質問なしで実装が完了できる粒度に仕上げる
- サブエージェントがさらにサブエージェントを呼ぶことは禁止（階層は 2 段まで: アーキテクト → プログラマー/テスト）
- 完了後はリレーエージェントに結果を返す（ユーザーには直接返答しない）

---

## タスク分割基準

### 複数タスクに分割する（いずれか該当）

- 変更が異なる関心事に跨がる（例: マイグレーション / UI / テスト）
- ファイルが重複せず並列実行できる
- 段階的な検証が必要（例: DB 確認後に UI 着手）
- 変更ファイルが 3 以上 かつ それぞれ独立している

### 単一タスクにまとめる（すべて該当）

- 変更ファイルが 1〜2 ファイルで密結合
- 変更量が目安 50 行以下
- 部分デプロイ不可（スキーマ変更と API が不可分など）

---

## 実装フロー（アーキテクトエージェントが実行）

```
1. 指示を受け取る
2. blueprint/tasks/todo/ にタスクファイルを作成（コードは書かない）
3. プログラマーエージェントを Agent ツールで呼び出し、タスクファイルを渡す
4. 完了後に diff をレビュー
5. タスクファイルを done/ へ移動
6. blueprint/adr・defines・ヘルプページを更新
7. リレーエージェントに完了を報告（commit/push はリレーエージェント経由でユーザーに確認）
```

---

## タスクファイル仕様

- 配置: `blueprint/tasks/todo/` (着手予定) / `pending/` (バックログ) / `done/` (完了)
- ファイル名: `task-YYYYMMDDNNN-<slug>.md`（例: `task-20260322001-inquiry-reply-templates.md`）
- `blueprint/tasks/` は `.gitignore` によりソース管理対象外（ローカル専用）

### 必須項目

```markdown
---
status: todo
priority: high | medium | low
conflict_risk: high | low
depends_on: []
---

# task-YYYYMMDDNNN — タイトル

## 概要
## 変更ファイル一覧
## 実装手順
## 注意
```

---

## プログラマーエージェントへの委譲ルール

- タスクファイルなしで呼び出さない
- 「タスクファイルに書かれていない設計変更・リファクタは行わないこと」と明記して渡す
- ビルド確認（`node_modules/.bin/next build`）まで実施させる
- マイグレーションファイルの**作成は OK**、`supabase db push` の実行は**禁止**

### 品質ゲート（プログラマーエージェントが守る最低条件）

1. **ビルドエラーがないこと** — `node_modules/.bin/next build` が成功すること
2. **E2E テストが通っていること** — メジャーバージョンアップ時にテストエージェントが確認

---

## バージョン管理とテスト戦略

### バージョン定義

バージョンは `blueprint/milestones.md` で管理する。

| 種別 | 定義 | E2E テスト |
|---|---|---|
| **メジャー** | `blueprint/milestones.md` のマイルストーン達成時 | **必須**（テストエージェントを呼び出す） |
| **マイナー** | 個別機能の追加・改善 | 任意 |
| **パッチ** | バグ修正・細部 UI 修正 | 不要 |

### メジャーバージョンアップ時のフロー

```
1. blueprint/milestones.md のマイルストーンを達成したと判断
2. アーキテクトエージェントがテストエージェントを呼び出す
3. E2E テストが全件通過したらバージョンをインクリメントして commit/push
4. テストが失敗した場合はプログラマーエージェントで修正してから再テスト
```

---

## ワークフロールール

- **Git push 前に必ずユーザーに確認する**: 「ステージングへ直接反映」か「ローカルで先に検証」かを聞く
- main ブランチへの push で Vercel に自動デプロイされる
- **push 前に必ず blueprint を更新してからコミットすること**
  - `blueprint/adr/`: 新規 ADR を `YYYYMMDD-<slug>.md` で作成
  - `blueprint/defines/`: スキーマ・UIルール変更があれば対応ファイルを更新

---

## 既知の地雷

### `router.push()` + `router.refresh()` の競合

`router.push()` の直後に `router.refresh()` を呼ぶと遷移が中断される。
遷移後のリフレッシュが必要に見えても `router.push()` のみにすること。

### Supabase Auth — メール確認

クラウド環境はデフォルトでメール確認必須 (`mailer_autoconfirm: false`)。
ログインエラー `Email not confirmed` は「パスワード誤り」ではなく専用メッセージで案内する。

### RLS — 自己参照による無限ループ

`team_members` テーブルの SELECT ポリシーで同テーブルを参照すると `infinite recursion detected` になる。
回避策: `SECURITY DEFINER` 関数 (`is_member_of_team()`) を経由する。
